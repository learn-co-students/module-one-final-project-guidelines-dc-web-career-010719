require_relative '../config/environment'

# describe "#welcome_user" do
#
#   it "returns a user object if the user already exists" do
#     allow($stdin).to receive(:gets).and_return('sharon')
#     name = $stdin.gets
#     expect(welcome_user)
#   end
#
#   it "creates a new user object if there is no user by that name" do
#   end
#
# end

# describe "#format_recipe" do
#
#   let (:recipe) {Recipe.new(name:"Gin and Tonic")}
#
#   it "takes in a recipe object and outputs the formatted recipe" do
#     expect



# RSpec.describe do
#   describe 'capture_name' do
#     it 'returns foo as input' do
#       allow($stdin).to receive(:gets).and_return('foo')
#       name = $stdin.gets
#
#       expect(name).to eq('food')
#     end
#   end
# end
